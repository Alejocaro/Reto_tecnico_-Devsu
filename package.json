{
  "name": "prueba-tecnica-qa",
  "version": "1.0.0",
  "description": "Prueba Técnica QA - Automatización E2E y Pruebas API",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:demoblaze": "cypress run --spec 'cypress/e2e/demoblaze/**'",
    "cy:run:api": "cypress run --spec 'cypress/e2e/api/**'",
    "cy:run:report": "node -e \"require('fs').rmSync('cypress/reports', { recursive: true, force: true })\" && cypress run --reporter cypress-mochawesome-reporter",
    "test:all:report": "npm run cy:run:report",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "cypress": "^13.17.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "dotenv": "^17.4.2",
    "eslint": "^10.4.0"
  }
}
